import Login from "@/components/auth/Login";

export const metadata = {
    title: "Login | DocAppoint",
    description: "Log in to your DocAppoint account to book and manage doctor appointments easily.",
};

const LoginPage = () => {
    return (
        <Login />
    );
};

export default LoginPage;