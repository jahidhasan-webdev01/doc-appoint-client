import Register from "@/components/auth/Register";

export const metadata = {
    title: "Register | DocAppoint",
    description: "Register in to your DocAppoint account to book and manage doctor appointments easily.",
};

const RegisterPage = () => {
    return (
        <Register />
    );
};

export default RegisterPage;